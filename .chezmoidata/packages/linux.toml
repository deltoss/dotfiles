# Linux package definitions (stub).
#
# This repo is Windows-first with minor Linux support, so there are no Linux
# package entries yet. This file mirrors packages/windows.toml and gives Linux
# installs a home when they're needed.
#
# Use the same schema as windows.toml, but key entries under [[packages.linux]]
# and tag them the same way ("all" | "personal" | "work"). Example:
#
#   [[packages.linux]]
#   name = "ripgrep"
#   description = "Line-oriented recursive regex search"
#   tool = "rg"                  # skip install if `which rg` already resolves
#   tags = ["all"]
#   package_type = "commands"    # or a distro package manager
#   commands = ["cargo binstall -y ripgrep"]
#
# Until real entries are added, this file is comment-only and contributes no data.
