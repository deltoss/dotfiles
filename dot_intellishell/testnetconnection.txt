# --------------------------------------------------------------
#   Commands
# --------------------------------------------------------------

# Test port connectivity
Test-NetConnection -ComputerName {{hostname}} -Port {{port}}
