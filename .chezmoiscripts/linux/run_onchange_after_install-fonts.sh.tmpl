{{ if hasPrefix "linux" .osid -}}
#!/bin/bash

DIR="~/nerd-fonts"
if [ -d "$DIR" ]; then
  rm -rf "$DIR"
fi

git clone --depth 1 https://github.com/ryanoasis/nerd-fonts.git ~/nerd-fonts
cd ~/nerd-fonts/
{{- range .fonts.linux }}
./install.sh {{ . }}
{{- end }}

{{ end -}}
