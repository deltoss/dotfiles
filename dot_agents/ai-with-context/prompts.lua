return {
  rules = {
    -- Rules are checked top to bottom. The first matching rule wins.
    -- Omit matchers you do not need. Every provided matcher must match.
    -- {
    --   process = "example-app",
    --   class = "example-class",
    --   workspace = "1",
    --   title_contains = "Example title",
    --   prompt = "Application-specific instructions for Pi.",
    -- },
  },
}
